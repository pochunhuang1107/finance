# PostgreSQL client
psycopg2-binary

# AWS SDK for Python (for S3, etc.)
boto3

# Data processing libraries
pandas
numpy

# Airflow dependencies (for running DAGs)
apache-airflow

# API Requests (for fetching stock data from Polygon)
requests

# Data visualization (optional)
matplotlib
seaborn